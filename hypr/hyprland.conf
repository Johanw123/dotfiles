

# #######################################################################################
# AUTOGENERATED HYPRLAND CONFIG.
# EDIT THIS CONFIG ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hypr.land/Configuring/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf



################
### MONITORS ###
################

# See https://wiki.hypr.land/Configuring/Monitors/
# monitor=,preferred,auto,auto

# Mac internal
monitor=eDP-1,preffered,auto,1.6

# Work pc built in
# monitor=eDP-2,2560x1600@240,-1600x0, auto
monitor=eDP-2,2560x1600@240,-2560x0,auto

# Work laptop built in
# monitor=eDP-2,2560x1600@240,0x0,auto
monitor=desc:Acer Technologies XB271HU,2560x1440@165,0x0,auto
monitor=desc:Dell Inc. DELL U2717D J0XYN952165S,2560x1440@60,2560x0,auto


# Home PC
# monitor=desc:Dell Inc. DELL U2725QE 2FJM934, 3840x2160@120,-2560x0,auto
# monitor=desc:Acer Technologies XB271HU T4TEE0028502, 2560x1440@60,0x0,auto


monitor=desc:Dell Inc. DELL U2725QE 2FJM934, 3840x2160@120,-0x0,auto
monitor=desc:Acer Technologies XB271HU T4TEE0028502, 2560x1440@60,2560x0,auto

workspace=name:1,monitor:desc:Acer Technologies XB271HU

#from this setting, check for more good settings here for gaming?
#https://github.com/Cybersnake223/Hypr/blob/main/.config/hypr/hyprland.conf
xwayland {
  force_zero_scaling = true
  use_nearest_neighbor = true
  enabled = true
}


###################
### MY PROGRAMS ###
###################

# See https://wiki.hypr.land/Configuring/Keywords/

# Set programs that you use
$terminal = kitty -o shell=fish
$fileManager = dolphin
$menu = wofi --show drun
$browser = firefox

#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# exec-once = $terminal
# exec-once = nm-applet &
# exec-once = waybar & hyprpaper & firefox
# exec-once = hyprpanel

exec-once = systemctl --user start gnome-keyring-daemon.service
exec-once = ibus-daemon -rxRd
exec-once = hyprctl dispatch worspace 1
exec-once = bash -c "wl-paste --watch cliphist store &"
exec-once = /usr/lib/mate-polkit/polkit-mate-authentication-agent-1
exec-once = dms run
exec-once = hyprpm reload -n
exec-once = gsr-ui

#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hypr.land/Configuring/Environment-variables/

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24
env = XDG_MENU_PREFIX,arch-
env = XDG_SESSION_TYPE,wayland

###################
### PERMISSIONS ###
###################

# See https://wiki.hypr.land/Configuring/Permissions/
# Please note permission changes here require a Hyprland restart and are not applied on-the-fly
# for security reasons

# ecosystem {
#   enforce_permissions = 1
# }

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hypr.land/Configuring/Variables/

# https://wiki.hypr.land/Configuring/Variables/#general
general {
    gaps_in = 5
    gaps_out = 20

    border_size = 2

    # https://wiki.hypr.land/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hypr.land/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
}

# https://wiki.hypr.land/Configuring/Variables/#decoration
decoration {
    rounding = 10
    rounding_power = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hypr.land/Configuring/Variables/#blur
    # blur {
    #     enabled = true
    #     size = 5
    #     passes = 5
    #     input_methods = true
    #     input_methods_ignorealpha = 0
    #     # vibrancy_darkness = 0.5
    #
    # new_optimizations = true
    # # xray = true
    #      ignore_opacity = true
    #     vibrancy = 0.1696
    # }
  blur {
    enabled =  true
    size = 8
    passes = 4
    ignore_opacity = true
    brightness = 1.0
    xray = false
    vibrancy = 0.3
    vibrancy_darkness = 0.05
    contrast = 1.0
    new_optimizations = true
    noise = 0
    popups = true
  }
}

source = ./animation.conf
# https://wiki.hypr.land/Configuring/Variables/#animations
# animations {
#     enabled = yes, please :)
#
#     # Default animations, see https://wiki.hypr.land/Configuring/Animations/ for more
#
#     bezier = easeOutQuint,0.23,1,0.32,1
#     bezier = easeInOutCubic,0.65,0.05,0.36,1
#     bezier = linear,0,0,1,1
#     bezier = almostLinear,0.5,0.5,0.75,1.0
#     bezier = quick,0.15,0,0.1,1
#
#     animation = global, 1, 10, default
#     animation = border, 1, 5.39, easeOutQuint
#     animation = windows, 1, 4.79, easeOutQuint
#     animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
#     animation = windowsOut, 1, 1.49, linear, popin 87%
#     animation = fadeIn, 1, 1.73, almostLinear
#     animation = fadeOut, 1, 1.46, almostLinear
#     animation = fade, 1, 3.03, quick
#     animation = layers, 1, 3.81, easeOutQuint
#     animation = layersIn, 1, 4, easeOutQuint, fade
#     animation = layersOut, 1, 1.5, linear, fade
#     animation = fadeLayersIn, 1, 1.79, almostLinear
#     animation = fadeLayersOut, 1, 1.39, almostLinear
#     animation = workspaces, 1, 1.94, almostLinear, fade
#     animation = workspacesIn, 1, 1.21, almostLinear, fade
#     animation = workspacesOut, 1, 1.94, almostLinear, fade
# }

# Ref https://wiki.hypr.land/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrule = rounding 0, floating:0, onworkspace:w[tv1]
# windowrule = bordersize 0, floating:0, onworkspace:f[1]
# windowrule = rounding 0, floating:0, onworkspace:f[1]

# See https://wiki.hypr.land/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hypr.land/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hypr.land/Configuring/Variables/#misc
misc {
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(
    session_lock_xray = 1
}


#############
### INPUT ###
#############

# https://wiki.hypr.land/Configuring/Variables/#input
input {
    kb_layout = us,se
    kb_variant =
    kb_model =
    kb_options=grp:win_space_toggle
    kb_rules =
    repeat_delay = 300
    repeat_rate = 80

    # accel_profile=flat
    follow_mouse=1
    # force_no_accel=1
    # sensitivity = -1.0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = true
    }
}

cursor {
    no_hardware_cursors = true
} 

# https://wiki.hypr.land/Configuring/Variables/#gestures
# gestures {
#     workspace_swipe = false
# }

# Example per-device config
# See https://wiki.hypr.land/Configuring/Keywords/#per-device-input-configs for more
device {
    name = logitech-usb-laser-mouse
    sensitivity = 0.0
}

plugin {
    split-monitor-workspaces {
        count = 5
        keep_focused = 1
        enable_notifications = 0
        enable_persistent_workspaces = 1
    }
}


###################
### KEYBINDINGS ###
###################

# See https://wiki.hypr.land/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hypr.land/Configuring/Binds/ for more
bind = $mainMod, T, exec, $terminal
bind = $mainMod, Return, exec, $terminal
bind = $mainMod, Q, killactive,
bind = $mainMod, M, exit,
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, V, togglefloating,
bind = $mainMod, R, exec, $menu
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, togglesplit, # dwindle
bind = $mainMod, B, exec, $browser

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
# bind = $mainMod, 1, workspace, 1
# bind = $mainMod, 2, workspace, 2
# bind = $mainMod, 3, workspace, 3
# bind = $mainMod, 4, workspace, 4
# bind = $mainMod, 5, workspace, 5
# bind = $mainMod, 6, workspace, 6
# bind = $mainMod, 7, workspace, 7
# bind = $mainMod, 8, workspace, 8
# bind = $mainMod, 9, workspace, 9
# bind = $mainMod, 0, workspace, 10
#
# # Move active window to a workspace with mainMod + SHIFT + [0-9]
# bind = $mainMod SHIFT, 1, movetoworkspace, 1
# bind = $mainMod SHIFT, 2, movetoworkspace, 2
# bind = $mainMod SHIFT, 3, movetoworkspace, 3
# bind = $mainMod SHIFT, 4, movetoworkspace, 4
# bind = $mainMod SHIFT, 5, movetoworkspace, 5
# bind = $mainMod SHIFT, 6, movetoworkspace, 6
# bind = $mainMod SHIFT, 7, movetoworkspace, 7
# bind = $mainMod SHIFT, 8, movetoworkspace, 8
# bind = $mainMod SHIFT, 9, movetoworkspace, 9
# bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Switch workspaces with mainMod + [0-5]
bind = $mainMod, 1, split-workspace, 1
bind = $mainMod, 2, split-workspace, 2
bind = $mainMod, 3, split-workspace, 3
bind = $mainMod, 4, split-workspace, 4
bind = $mainMod, 5, split-workspace, 5

# Move active window to a workspace with mainMod + SHIFT + [0-5]
bind = $mainMod SHIFT, 1, split-movetoworkspacesilent, 1
bind = $mainMod SHIFT, 2, split-movetoworkspacesilent, 2
bind = $mainMod SHIFT, 3, split-movetoworkspacesilent, 3
bind = $mainMod SHIFT, 4, split-movetoworkspacesilent, 4
bind = $mainMod SHIFT, 5, split-movetoworkspacesilent, 5

# Example special workspace (scratchpad)
# bind = $mainMod, S, togglespecialworkspace, magic
# bind = $mainMod SHIFT, S, movetoworkspace, special:magic
bind = $mainMod SHIFT, S, exec, hyprshot -m region --clipboard-only 
bind = $mainMod, PRINT, exec, hyprshot -m window


# gpu-screen-recorder -w region -region $(slurp -f "%wx%h+%x+%y") -o video.mp4


bind = $mainMod, L, exec, hyprlock
bind = $mainMod SHIFT, C, exec, hyprpicker --autocopy

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

# Application and system controls
# bind = SUPER, Space, exec, dms ipc call spotlight toggle
bind = SUPER, D, exec, dms ipc call spotlight toggle
# bind = SUPER, V, exec, dms ipc call clipboard toggle
# bind = SUPER, M, exec, dms ipc call processlist toggle
# bind = SUPER, N, exec, dms ipc call notifications toggle
bind = SUPER, comma, exec, dms ipc call settings toggle
# bind = SUPER, P, exec, dms ipc call notepad toggle
bind = SUPERALT, L, exec, dms ipc call lock lock
bind = SUPER, X, exec, dms ipc call powermenu toggle
bind = SUPER, C, exec, dms ipc call control-center toggle 

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hypr.land/Configuring/Window-Rules/ for more
# See https://wiki.hypr.land/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule
# windowrule = float,class:^(kitty)$,title:^(kitty)$

# Ignore maximize requests from apps. You'll probably like this.
windowrule = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland
windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0


windowrule = fullscreen, title:^(DotA)$


bind = SUPER, z, easymotion, action:hyprctl dispatch focuswindow address:{}
bind=SUPER,F,fullscreen

bind = $mainMod, F1, exec, ~/.config/hypr/scripts/gamemode.sh

bind = $mainMod, F2, exec, dms kill
bind = $mainMod, F3, exec, dms restart

plugin {
  easymotion {
    #font size of the text
    textsize=70
      #color of the text, takes standard hyprland color format
    textcolor=rgba(ffffffff)

    #background color of the label box. alpha is respected
    bgcolor=rgba(000000ff)

    #enable blur. The bgcolor alpha must be at least semi-transparent.
    blur=0

    #Set blur alpha value. Blur must be enabled (float value)
    blurA=0.3

    #Set xray. Blur must be enabled
    xray=1

    rounding=0
    #size of the border around the label box. A border size of zero disables border rendering.
    bordersize=0

    #color of the border. takes the same format as hyprland's border (so it can be a gradient)
    bordercolor=rgba(ffffffff)
    }
}

# Hyprland window rules

# Firefox rules
# windowrule = opacity 1.0 1.0 override, class:^(firefox),title:(.*)(Netflix)(.*)
# windowrule = opacity 1.0 1.0 override , class:(firefox), title:(.*)(YouTube)(.*)
# windowrule = opacity 1.0 1.0 override, class:^(firefox),title:(.*)(Prime Video)(.*)
# windowrule = opacity 1.0 1.0 override, class:^(firefox),title:(.*)(Disney+)(.*)
#
# # Google-Chrome rules
# windowrule = opacity 1.0 1.0 override, class:^(google-chrome),title:(.*)(Netflix)(.*)
# windowrule = opacity 1.0 1.0 override , class:(google-chrome), title:(.*)(YouTube)(.*)
# windowrule = opacity 1.0 1.0 override, class:^(google-chrome),title:(.*)(Prime Video)(.*)
# windowrule = opacity 1.0 1.0 override, class:^(google-chrome),title:(.*)(Disney+)(.*)
#
# # Opacity rules
# windowrule = opacity 1.0 1.0 override, class:^(blender),title:(Blender)
# windowrule = opacity 1.0 1.0 override, class:^(kitty),title:(btop)
# windowrule = opacity 1.0 1.0 override, class:^(kitty),title:(nvtop)
#
# # Floating windows
# windowrule = float, opacity 1.0 1.0 override, class:^(galculator)
# windowrule = float, opacity 1.0 1.0 override, class:blender, title:Preferences

# windowrule = opacity 1.0 0.5 override, class:^(firefox)


# darkwindow:shader[tintRed] {
#     from = tint
#     args = tintColor=[1 0 0] tintStrength=0.1
# }

darkwindow:shader[test] {
    from = chromakey
    args = bkg = [0 0 0] similarity = 0.2 amount = 1 targetOpacity = 0.8
}

# windowrulev2 = plugin:shadewindow test,class:^(firefox)
# windowrulev2 = plugin:shadewindow test, class:^(.*)$ 
# windowrule = opacity 0.9 0.9 override, class:^(firefox)
windowrule = opacity 0.89 override 0.89 override, class:.* 


# class:^()$ - nothing
#class:^()$ - nothing
#class:^(.*)$ - everything, altho i'm not sure if it includes nothing case too
#class:^(.*team) - everything ending with team
#class:^([Ss]team) - Steam or steam 
